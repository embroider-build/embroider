{
    "private": true,
    "workspaces": ["packages/*"],
    "scripts": {
      "compile": "tsc",
      "lint": "tslint --project .",
      "clean": "git clean -x -f",
      "test": "qunit packages/*/tests"
    }
  }
