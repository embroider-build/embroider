{
    "private": true,
    "workspaces": ["packages/*", "test-packages/*", "types/babel-core"],
    "scripts": {
      "compile": "tsc",
      "lint": "tslint --project .",
      "clean": "git clean -x -f",
      "test": "qunit packages/*/tests"
    }
  }
