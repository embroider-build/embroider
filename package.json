{
    "private": true,
    "workspaces": ["packages/*", "test-packages/*", "types/babel-core"],
    "scripts": {
      "compile": "tsc",
      "lint": "tslint --project .",
      "clean": "git clean -x -f",
      "node-test": "qunit packages/*/tests",
      "test": "./scripts/test.sh"
    }
  }
