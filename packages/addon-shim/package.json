{
  "name": "@embroider/addon-shim",
  "version": "1.8.6",
  "description": "Make v2 addons work in non-Embroider apps.",
  "keywords": [],
  "main": "./src/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/embroider-build/embroider.git",
    "directory": "packages/addon-shim"
  },
  "license": "MIT",
  "author": "",
  "directories": {
    "doc": "doc",
    "test": "tests"
  },
  "scripts": {
    "build": "concurrently 'npm:build:*'",
    "build:js": "rollup -c ./rollup.config.mjs",
    "build:types": "build-types",
    "start": "pnpm build:js --watch",
    "prepack": "pnpm -w exec turbo --filter '@embroider/addon-shim' build"
  },
  "dependencies": {
    "@embroider/shared-internals": "workspace:^",
    "broccoli-funnel": "^3.0.8",
    "semver": "^7.3.8"
  },
  "devDependencies": {
    "@embroider/build": "workspace:*",
    "@types/semver": "^7.3.6",
    "broccoli-node-api": "^1.7.0",
    "typescript": "^5.1.6",
    "webpack": "^5"
  },
  "engines": {
    "node": "12.* || 14.* || >= 16"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
